# @boilerplate-utils/react

implement `cycler`, and all of the `nester` components. Finish a zod-generator function that works with nester & cycler components.

## Ethos

never want to use hooks - the only thing exposed to the user is Observables. And always discourage subjects - users should be in Observable-land only
